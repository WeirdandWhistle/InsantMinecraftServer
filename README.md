# InsantMinecraftServer
spawn a minecraft server instantly using cloudflared tunnels to make it publicly accesible

# TODO
I have a backlog of too many project todo this right now but I think its a super cool idea to make a minecraft server app that can spawn a server and then forward it to the public using cloudflared. Main problem is everybody would either need a client or cloudflared installed and run a `cloudflared accses tcp [etc...]` command. But still something i want to make in the nead future. this repo is mainly a reminder until then.

# Stack
the stack how to work on all OSs `Linux, mac, windows(booo!)` so probaly java or an electron app. need to be able to run command. I guess it could be a client specific install scirpt `.sh / .bat` to install java and then the main program.
